packages:
  - 'apps/*'
  - 'packages/*'
  # ⚠️ Uçtan uca testler. Workspace'e alınması `@vt/db`nin `workspace:*` ile
  #    çözülebilmesi için ZORUNLU. Buna rağmen `pnpm test` (turbo run test)
  #    onu ÇALIŞTIRMAZ: paket `test` betiği sağlamaz, koşum betiğinin adı
  #    `e2e`dir (bkz. e2e/package.json). E2E ayakta bir API ister.
  - 'e2e'
