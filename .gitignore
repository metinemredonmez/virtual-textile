# ── Gizli / ticari dokümanlar — ASLA commit edilmez ──────────────────────
*.pdf
*.docx
*.doc
*.xlsx
/sozlesme/
/contracts-legal/
MVP_UYGULAMA_PLANI.md
Ekran*.png

# ── Secrets ───────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
service-account*.json
gcp-*.json

# ── Bağımlılıklar & build ────────────────────────────────────────────────
node_modules/
dist/
build/
.next/
out/
*.tsbuildinfo
.turbo/
coverage/
.nyc_output/

# ── Prisma ───────────────────────────────────────────────────────────────
packages/db/generated/
prisma/*.db
prisma/*.db-journal

# ── Test / araç çıktıları ────────────────────────────────────────────────
playwright-report/
test-results/
.vitest/
*.log
npm-debug.log*
pnpm-debug.log*

# ── Editör & OS ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json

# ── Yerel geliştirme ─────────────────────────────────────────────────────
/tmp/
/uploads/
.pgdata/
.redisdata/

# pnpm
pnpm-lock.yaml.bak

# ⚠️ Next.js bu dosyayı HER DERLEMEDE yeniden yazar (içeriği .next/types
#    yollarına göre değişir). İzlenirse çalışma ağacı hiçbir zaman temiz
#    olmaz ve dağıtım betiği her koşumda gereksiz yere onay ister.
#    Next resmî şablonunda da yok sayılıyor; "bu dosyayı düzenlemeyin" diyor.
apps/web/next-env.d.ts

# ⚠️ ÜRETİLEN DEMO ÜRÜN GÖRSELLERİ — depoya girmez.
#    `pnpm --filter @vt/db seed:varlik` ile yeniden üretilir (FAL_KEY gerekir).
#    Gerekçe: 298 gerçek fotoğraf ~35 MB; sentetik silüetler 0,3 MB idi ve
#    bu yüzden commit edilebiliyordu. Eksik dosya sessiz kalmaz —
#    `seed/gorsel.ts` okurken atar.
packages/db/prisma/seed-assets/urunler/
packages/db/prisma/seed-assets/vitrin/
