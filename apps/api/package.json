{
  "name": "@vt/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "//dev": "⚠️ SADECE GELİŞTİRME. Uygulama .env OKUMAZ (main.ts → loadEnv yalnızca process.env'e bakar) ve üretimde okumamalıdır: sunucuda ortam /etc/virtual-textile/api.env dosyasından PM2 ile verilir. Bu sarmalayıcı olmadan `pnpm --filter @vt/api dev` ORTAM DOĞRULAMASINDA DÜŞER — E2E'nin 'sunucuyu playwright başlatsın' yolu bu yüzden hiç çalışmıyordu ve arıza yalnızca elle başlatılmış artakalan bir sunucu olmadığında görünüyordu. ⚠️ `dotenv` var olan değişkenin ÜZERİNE YAZMAZ: playwright'ın webServer.env ile verdiği IYZICO_* değerleri kazanır, kök .env'deki boş değerler onları ezmez.",
    "dev": "dotenv -e ../../.env -- nest start --watch",
    "build": "nest build",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.1",
    "@nestjs/core": "^11.0.1",
    "@nestjs/jwt": "^11.0.2",
    "@nestjs/platform-express": "^11.0.1",
    "@vt/adapters": "workspace:*",
    "@vt/config": "workspace:*",
    "@vt/contracts": "workspace:*",
    "@vt/db": "workspace:*",
    "argon2": "^0.41.1",
    "blurhash": "^2.0.5",
    "cookie-parser": "^1.4.7",
    "helmet": "^8.0.0",
    "ioredis": "^5.4.2",
    "pino": "^9.6.0",
    "pino-http": "^10.4.0",
    "rate-limiter-flexible": "^5.0.5",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "sharp": "^0.35.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/testing": "^11.0.1",
    "@types/cookie-parser": "^1.4.10",
    "@types/express": "^5.0.0",
    "@types/express-serve-static-core": "^5.1.3",
    "@types/node": "^20.17.10",
    "@types/supertest": "^6.0.2",
    "dotenv-cli": "^11.0.0",
    "pino-pretty": "^13.0.0",
    "supertest": "^7.0.0",
    "typescript": "^5.7.2",
    "vitest": "^3.2.7"
  }
}
